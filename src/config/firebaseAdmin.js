/**
 * firebaseAdmin.js — Firebase removed.
 * Google identity is now verified via the Google OAuth2 userinfo endpoint.
 * Stub exports kept so any residual imports don't crash.
 */

export function initFirebaseAdmin() { return null }
export async function updateFirebaseUserPassword() { return { skipped: true } }
export async function getFirebaseUserByEmail() { return { skipped: true } }
export async function setUserAdminClaimByEmail() { return { skipped: true } }
export default null
